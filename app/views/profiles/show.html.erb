
<div class="container">
  <div id="wrapper">
  <div class="row">
    <div class="col-md-12 capitalize">
    <div class="instructor">
  <h1 class="top huge text-center">Instructor Profile</h1>
  <br/>
      <% if !@user.image.url.nil? %>
      <%=image_tag @user.image.url(:large) %>
      <% end %>
<br/>
<br/>
     <div id="name">
      <p class="info"><strong>Name:</strong>
        <%=@user.first_name + " " + @user.last_name%>
      </p>
      <p class="info"><strong>Interests:</strong>
        <%=@user.interests%>
      </p>
      <p class="info"><strong>Fitness Background:</strong>
        <%=@user.fitness_background%>
      </p>
    </div>
  </div>
</div>
<div id="tableinfo">
<div class="col-md-12">
  <h3 class="top biggest">My Classes</h3>
  <br/>
      <div class="table-responsive text-center">
        <table class="table-striped capitalize">
          <tr>
            <td class="octin"><strong>Class Name</strong></td>
            <td class="octin"class="octin"><strong>Date</strong></td>
            <td class="octin"><strong>Time</strong></td>
            <td class="octin"><strong>Location</strong></td>
            <td class="octin"><strong>Duration</strong></td>
            <td class="octin"><strong>Class Description</strong></td>
           </tr>
          <% @workouts.each do |workout| %>
           <tr>
             <td><%= workout.name %></td>
             <td><%= workout.date.strftime("%m/%d/%y") %></td>
             <td><%= workout.time.strftime('%r') %></td>
             <td><%= workout.location %></td>
             <td><%= workout.duration %></td>
             <td><%= workout.description %></td>
             <%if current_user.id == @user.id%>
             <td><%= link_to 'Edit', edit_workout_path(workout) %></td>
             <td><%= link_to 'Destroy', workout, method: :delete, data: { confirm: 'Are you sure?' } %></td>
             <%end%>
           </tr>
          <% end %>
         </table>
       </div>
      </div>
     </div>
    </div>
   </div>
